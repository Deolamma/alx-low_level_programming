#include "main.h"
#include <string.h>
/**
 * _strlen - determines the length of a string
 * irrespective of the size of the array
 *
 * @s: string param
 *
 * Return: returns an int which is the length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
