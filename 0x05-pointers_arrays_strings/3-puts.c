#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string and inserts a new line
 * @str: string parameter to be printed out
 *
 * Return: Returns void (Always 0)
 */
void _puts(char *str)
{
	puts(str);
}
